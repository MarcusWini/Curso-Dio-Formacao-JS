// 1-
function escrevaSeuNome(nome){
    return nome;
}
// 2 -
function verificarIdade(idade){
// (if/else)Operacao Ternaria (condicao) ? 'Valor pra verdadeiro' : 'Valor para falso';
const maioridade = (idade >= 18) ? 'Maior de Idade' : 'Menor de idade';
return maioridade; }
// 3 -
function invocaFuncao(nome, idade){
    let nomefun1 = escrevaSeuNome(nome);
    let idadefun2 = verificarIdade(idade);
    return console.log( nomefun1 + ' é ' + idadefun2);
}
// 4 -
function calculaPreco(precoEtiqueta, formaDePagamento){
    if (formaDePagamento ===1) return precoEtiqueta - (precoEtiqueta*0.1);
    else if (formaDePagamento ===2) return precoEtiqueta - (precoEtiqueta*0.15);
    else if (formaDePagamento ===3) return precoEtiqueta + ' em 2x de R$'+ precoEtiqueta/2;
    else if (formaDePagamento ===4) return precoEtiqueta + (precoEtiqueta*0.10);
    else return'0,00 => Opcao invalida';
}
//funcao main
(function (){
   console.log('Escolha o exercicio: \n 1 - Escreva seu Nome \n 2 - Funcao verificar idade \n 3 - Invocando Funcao Dentro de outra Funcao \n 4 - Funcao para Caucular Preco' );
    let opcao=4;
    
    if (opcao == 1)return console.log(escrevaSeuNome('Marcus Wincius Reinisch'));
    else if (opcao == 2)return console.log(verificarIdade('36'));
    else if (opcao == 3)return invocaFuncao('jao', '15');
    else if (opcao == 4)return console.log('1-Debito\n2-Pix ou Cash\n3-2x sem juros\n4-Acima de 2x\nValor Final:R$'+calculaPreco(100,5));
    else console.log('Opcao invalida');
})();